1、查看字符集
show character set;

2、查看使用状态
status；

编译支持gb2312字符集
--with-charset=gb2312

编译支持gbk字符集
--with-charset=gbk

创建GBK编码数据库
CREATE DATABASE `new0573ren` /*!40100 DEFAULT CHARACTER SET gbk */;

给账户授权
grant all privileges on dzx.* to dzx@localhost identified by "fK65taY1fQ*GI";

撤销用户权限
revoke all privileges on dzx.* from dzx@localhost; 


查看慢查询状态
mysql>show variables like '%slow%';
动态打开
mysql>set global slow_query_log=on;

查看表结构
show create table pre_common_session\G；

查询数据引擎
show engines;

修改mysql的最大连接数
set GLOBAL max_connections=200;

查看mysql的最大连接数
show variables like '%max_connections%';


创建索引
alter table pre_forum_thread add key(special);
创建联合索引
alter table pre_forum_thread add key(authorid,first);

删除索引
alter table pre_forum_thread drop index special
ALTER TABLE table_name DROP INDEX index_name(索引名)

优化mysql数据表
optimize table tbl_name;

查看 MySQL 用户权限:
查看当前用户（自己）权限：
show grants;

查看其他 MySQL 用户权限：
show grants for dba@localhost;

重启mysql
mysqladmin -uroot -ppassword shutdown && /usr/local/app/mysql/bin/mysqld_safe --user=mysql &


导出数据 
导出库表结构：
mysqldump -uroot -p  --master-data --single-transaction --database db1 db2 db3 -d >t.sql

导出库表数据：
mysqldump -uroot -p  --master-data --single-transaction --database db1 db2 db3 -t >d.sql

导出数据库中某几个表
mysqldump -uroot -p --single-transaction db table1 table2 -d >t.sql     ##只导出表结构
mysqldump -uroot -p --single-transaction db table1 table2 -t >d.sql     ##只导出表数据

mysqldump -uroot -p --single-transaction db table1 table2 >d.sql     ##导出表结构是数据

导入数据库中某几个表
mysql -uroot -p db < d.sql	###无需写表名

导出数据库中导出EXECL 
select * from userinfo into outfile '/app/mysql/userinfo.xls';

select * from 某个表 into outfile  '文件路径和文件名';

查询某个表里有几条数据
select count(*) from 表名；

修改表的字段大小
alter table 表名称 modify 字段名称 字段类型 [是否允许非空];
alter table pre_forum_ratelog modify reason char(120);		##修改了pre_forum_ratelog表中的reason的char(120)大小

增加一个字段(非空，默认为0)
alter table 表名称 add 字段名称 字段类型 [是否允许非空];
alter table infos add extsfss tinyint not null default '0';	##在infos表里新增一个extsfss字段，类型为tinyint，非空，默认是0

删除字段：
alter table 表名 drop 字段名;﻿

修改某个字段的数据
修改jinhui_admin表中pwd字段的id为1的数据
update jinhui_admin set pwd = 'db121e8cddca2fb6a5e7' where id = 1;

